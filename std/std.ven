distinct std;

# Returns whether this value is true.
#
# ```
# ensure true?(0) is true;
# ensure true?("") is true;
# ensure true?([]) is true;
# ensure true?(false) is false;
# ```
fun<any> true? =
  $ is not false;
